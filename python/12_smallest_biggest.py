""" Find the smallest or biggest number in a list of numbers
    without using min()."""

def get_smallest(numbers):
    """ Find the smallest number."""
    if len(get_smallest) == 0:
        return None
